#include <iostream>
#include <string>
#include <algorithm>

using namespace std;


int main(void) {
	string a, b;
	cin >> a >> b;

	int aLen = a.size();
	int bLen = b.size();
	bool ten = false;
	string answer(max(aLen, bLen), '0');
	for (int i = 0; i < max(aLen, bLen); i++) {
		
	}
}